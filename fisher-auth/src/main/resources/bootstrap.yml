spring:
  application:
    name: fisher-auth-service
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yml
        group: AUTH_GROUP
  profiles:
    active: dev